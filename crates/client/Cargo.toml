[package]
name = "mnr-client"
description = "mnr — an RPC network for Monero: client library for wallets and backends (verification, multi-relayer agreement). Reserved; not yet implemented."
version.workspace = true
edition.workspace = true
license = "Apache-2.0"
repository.workspace = true
rust-version.workspace = true
readme = "README.md"

[dependencies]

[lints]
workspace = true
