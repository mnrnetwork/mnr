//! `mnr-relay` — the Stage 0 verified proxy. See `docs/stage0-mvp-plan.md` §6.

#![forbid(unsafe_code)]

fn main() {
    println!(
        "mnr-relay {} — not implemented yet",
        env!("CARGO_PKG_VERSION")
    );
}
