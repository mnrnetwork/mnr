//! `mnr-agent`: the small agent an operator runs beside `monerod` to join the network (Stage 2).
//!
//! This crate reserves the name for mnr — an RPC network for Monero
//! (<https://mnr.network>). The code lands here as the plan reaches it; see
//! the repository for what is being built right now.

#![forbid(unsafe_code)]
#![no_std]
