# @mnrnetwork/mnr-client

Client library for wallets and backends: verifies what a relayer returns (planned).

Part of [mnr — an RPC network for Monero](https://mnr.network); the same name
as the Rust crate `mnr-client` on crates.io. Nothing to use yet. Licence: Apache-2.0.
Contact: dev@mnr.network.
